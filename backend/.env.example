PORT=5000
JWT_SECRET=change-me-in-production
CLIENT_ORIGIN=http://localhost:5173,http://localhost:8080
DATABASE_PATH=./data/archive.db
UPLOAD_DIR=./uploads
MAX_USERS=0

# Railway (volume mounted at /srv/data) — see RAILWAY.md
# DATABASE_PATH=/srv/data/archive.db
# UPLOAD_DIR=/srv/data/uploads
# CLIENT_ORIGIN=https://YOUR-FRONTEND.vercel.app
